version: '3.4'

services:
  postgres:
    image: postgres:16.4-alpine3.20
    container_name: 'jpql'
    volumes:
      - jpql:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      - 'POSTGRES_USER=postgres'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_DB=persistence'

volumes:
  jpql:

networks:
  postgres:
    driver: bridge